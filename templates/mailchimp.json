{
  "parameters": {
    "resource": "{{ resource | default('campaign') }}",
    "operation": "{{ operation | default('getAll') }}"
  },
  "type": "n8n-nodes-base.mailchimp",
  "typeVersion": 1,
  "position": [0, 0]
}
