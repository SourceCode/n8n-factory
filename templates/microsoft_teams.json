{
  "parameters": {
    "resource": "{{ resource | default('channel') }}",
    "operation": "{{ operation | default('getAll') }}"
  },
  "type": "n8n-nodes-base.microsoftTeams",
  "typeVersion": 1,
  "position": [0, 0]
}
