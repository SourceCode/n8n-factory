{
  "parameters": {
    "resource": "{{ resource | default('customer') }}",
    "operation": "{{ operation | default('getAll') }}"
  },
  "type": "n8n-nodes-base.stripe",
  "typeVersion": 1,
  "position": [0, 0]
}
