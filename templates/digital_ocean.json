{
  "parameters": {
    "resource": "{{ resource | default('droplet') }}",
    "operation": "{{ operation | default('getAll') }}"
  },
  "type": "n8n-nodes-base.digitalOcean",
  "typeVersion": 1,
  "position": [0, 0]
}
