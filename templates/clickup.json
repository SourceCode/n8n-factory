{
  "parameters": {
    "resource": "{{ resource | default('task') }}",
    "operation": "{{ operation | default('getAll') }}"
  },
  "type": "n8n-nodes-base.clickUp",
  "typeVersion": 1,
  "position": [0, 0]
}
