{
  "parameters": {
    "mode": "{{ mode | default('fileToBinary') }}",
    "sourcePropertyName": "{{ sourcePropertyName }}",
    "destinationPropertyName": "{{ destinationPropertyName | default('data') }}"
  },
  "type": "n8n-nodes-base.moveBinaryData",
  "typeVersion": 1,
  "position": [0, 0]
}
