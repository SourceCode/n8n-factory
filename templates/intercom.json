{
  "parameters": {
    "resource": "{{ resource | default('contact') }}",
    "operation": "{{ operation | default('getAll') }}"
  },
  "type": "n8n-nodes-base.intercom",
  "typeVersion": 1,
  "position": [0, 0]
}
