{
  "parameters": {
    "operation": "{{ operation | default('resize') }}",
    "width": {{ width | default(800) }},
    "height": {{ height | default(600) }},
    "options": {}
  },
  "type": "n8n-nodes-base.editImage",
  "typeVersion": 1,
  "position": [0, 0]
}
