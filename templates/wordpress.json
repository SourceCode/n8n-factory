{
  "parameters": {
    "resource": "{{ resource | default('post') }}",
    "operation": "{{ operation | default('getAll') }}"
  },
  "type": "n8n-nodes-base.wordpress",
  "typeVersion": 1,
  "position": [0, 0]
}
